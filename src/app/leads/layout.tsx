import { BridalFlowLayout } from '@/components/layout/BridalFlowLayout'

export default function LeadsLayout({ children }: { children: React.ReactNode }) {
  return <BridalFlowLayout>{children}</BridalFlowLayout>
}
